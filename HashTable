import java.util.Arrays;

public class HashTable 
{
	String[] theArray;
	int arraySize;
	int itemsInArray = 0;
	
	HashTable(int size)
	{
		arraySize = size;
		theArray = new String[size];
		Arrays.fill(theArray, "-1");g
	}
}
